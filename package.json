{
  "name": "isolated-hooks",
  "version": "1.0.0",
  "main": "index.js",
  "author": "davidmfoley@gmail.com",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf ./lib/",
    "test": "TS_NODE_PROJECT=tsconfig.test.json mocha",
    "test:watch": "TS_NODE_PROJECT=tsconfig.test.json mocha -w",
    "prettier": "prettier -c '{src,test}/**/*.ts'",
    "fix:prettier": "prettier --write '{src,test}/**/*.ts'",
    "cover": "TS_NODE_PROJECT=tsconfig.test.json COVERAGE=1 nyc mocha && open coverage/index.html",
    "ci": "yarn build && yarn test && yarn prettier"
  },
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.1.3",
    "nyc": "^15.1.0",
    "prettier": "^2.1.2",
    "react": "^16.13.1",
    "source-map-support": "^0.5.19",
    "ts-mocha": "^7.0.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  },
  "mocha": {
    "extension": [
      "ts"
    ],
    "require": [
      "source-map-support/register",
      "ts-node/register"
    ],
    "watch-files": ["./src/**/*.ts", "./test/**/*.ts"]
  },
  "nyc": {
    "extension": [
      ".ts",
      ".tsx"
    ],
    "include": [
      "src/**/*.ts"
    ],
    "reporter": [
      "html"
    ],
    "all": true
  }
}
